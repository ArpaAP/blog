---
import type { CollectionEntry } from 'astro:content';

interface Props {
  post: CollectionEntry<'articles'>;
}

const { post } = Astro.props;
const date = new Date(post.data.published_at).toLocaleDateString('ko-KR', {
  year: 'numeric',
  month: 'short',
  day: 'numeric'
});
---

<a href={`/articles/${post.id}`} class="group flex flex-col sm:flex-row gap-2 sm:gap-8 py-6 border-b border-gray-100 dark:border-white/5 hover:bg-gray-50 dark:hover:bg-white/1 transition-colors -mx-4 px-4">
  <div class="sm:w-32 shrink-0 pt-1">
    <time datetime={post.data.published_at.toISOString()} class="text-sm text-gray-400 font-mono">{date}</time>
  </div>
  
  <div class="grow">
    <h3 class="text-lg font-bold text-gray-900 dark:text-gray-100 group-hover:text-brand-600 transition-colors mb-2">
      {post.data.title}
    </h3>
    <p class="text-gray-600 text-sm line-clamp-2 leading-relaxed">
      {post.data.excerpt}
    </p>
    
    {post.data.tags && post.data.tags.length > 0 && (
      <div class="mt-3 flex gap-2">
        {post.data.tags.map((tag) => (
          <span class="text-xs text-brand-600 font-medium bg-brand-50 dark:bg-brand-900 dark:text-brand-100 px-2 py-0.5 rounded">
            #{tag}
          </span>
        ))}
      </div>
    )}
  </div>
</a>
